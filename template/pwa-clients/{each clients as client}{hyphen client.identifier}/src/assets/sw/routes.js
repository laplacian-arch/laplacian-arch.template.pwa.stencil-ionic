/*
const routes = [{
  path: "/deck/all",
  handler: () => {
    const responseBody = []
    return new Response(JSON.stringify(responseBody, {status: 200}))
  }
}]
routes.forEach(route => {
  self.workbox.routing.registerRoute(route.path, route.handler)
})
*/

