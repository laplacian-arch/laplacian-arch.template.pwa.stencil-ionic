rootProject.name = "laplacian-arch.template.pwa.stencil-ionic"

pluginManagement {
    repositories {
        maven(url = "../mvn-repo/")
        maven(url = "https://raw.github.com/nabla-squared/mvn-repo/master/")
        gradlePluginPortal()
        jcenter()
    }
}

// @subprojects@
// Subproject definitions are inserted here.
// @subprojects@